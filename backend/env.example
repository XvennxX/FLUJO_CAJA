# Configuración de ambiente - Backend Sistema Flujo de Caja
# Copia este archivo como .env y ajusta los valores según tu ambiente

# Información de la aplicación
APP_NAME="Sistema de Flujo de Caja"
APP_VERSION="1.0.0"
DEBUG=true
ENVIRONMENT=development

# Base de datos PostgreSQL
DATABASE_URL="postgresql://flujo_user:flujo_pass_2025@localhost:5432/flujo_caja_db"

# Seguridad JWT
SECRET_KEY="flujo_caja_secret_key_super_secure_2025_CAMBIAR_EN_PRODUCCION"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS - URLs permitidas para el frontend
ALLOWED_HOSTS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# Configuración de archivos
UPLOAD_DIR="uploads"
REPORTS_DIR="reports"
MAX_FILE_SIZE=10485760

# Redis (opcional, para caché)
REDIS_URL="redis://localhost:6379"

# Email/SMTP (para notificaciones)
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASSWORD=""

# Configuración específica de producción
# En producción, configura estas variables de ambiente:
# - DATABASE_URL (con credenciales reales)
# - SECRET_KEY (generar una clave única y segura)
# - DEBUG=false
# - ALLOWED_HOSTS (URLs de producción)
